/** This file does not serve any configuration, but is replaced when serving or building other environments */

import { IEnvironment } from '@dsg/shared/utils/environment';

export const environment = {} as IEnvironment;
